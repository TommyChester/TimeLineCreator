<%= form_with(model: relative_time_space, local: true) do |form| %>
  <% if relative_time_space.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(relative_time_space.errors.count, "error") %> prohibited this relative_time_space from being saved:</h2>

      <ul>
      <% relative_time_space.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :epoch %>
    <%= form.text_field :epoch, id: :relative_time_space_epoch %>
  </div>

  <div class="field">
    <%= form.label :epochtime %>
    <%= form.text_field :epochtime, id: :relative_time_space_epochtime %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
